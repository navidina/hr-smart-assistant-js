# سند نیازمندی‌های محصول

این سند الزامات فنی و غیر فنی چت‌بات منابع انسانی مبتنی بر مدل `gpt-oss:20b` را پوشش می‌دهد. هدف، تضمین کیفیت پاسخ‌دهی، قابلیت نگه‌داری و تجربه‌ی کاربری هم‌سطح محصولات حرفه‌ای است.

## اهداف اصلی
- پاسخ‌گویی دقیق، قابل توضیح و قابل اتکا به سؤالات پرسنلی، مزایا و سیاست‌ها.
- پشتیبانی از پرسش‌های استنتاجی (تعداد نیروها، نقش‌ها، فیلترهای حوزه/شخص) بدون شرط‌های تو در تو.
- حفظ عملکرد در محیط لوکال با امکان دسترسی از طریق شبکه داخلی سازمان.
- فراهم کردن رابط کاربری مدرن، سریع و واکنش‌گرا با امکاناتی مشابه سرویس‌های گفت‌وگویی شناخته‌شده.

## نیازمندی‌های عملکردی (Functional)
1. **پرسش و پاسخ چندمنظوره**
   - تحلیل نیت سؤالات (افراد، حوزه‌ها، مزایا، سفر، سیاست‌ها).
   - تولید پاسخ ساختارمند (لیست، جدول مختصر، خلاصه متنی) با استفاده از RAG و قواعد دترمینیستیک.
   - پشتیبانی از سوالات دنباله‌دار با حفظ حافظه‌ی مکالمه در صورت نیاز.
2. **سیاست‌ها و مزایا**
   - استخراج مزایای مناسب (عیدی، هدیه تولد، سفر و ...) از اسناد DOCX.
   - اولویت‌دهی به بخش‌های مرتبط با کلیدواژه‌های هم‌معنی فارسی.
3. **پرسنل و ساختار سازمانی**
   - جست‌وجوی فازی روی اسامی و عناوین شغلی با نرمال‌سازی فارسی.
   - ارائه‌ی آمار حوزه‌ها (تعداد کارکنان، نقش‌های غالب) و اطلاعات فردی (نام، پست، حوزه، کد).
4. **ثبت و ردیابی**
   - ثبت تمام پرسش‌ها و متادیتا (زمان، نیت، شناسه پاسخ) در `question_log.jsonl`.
   - درگاه `/feedback` برای ذخیره‌ی واکنش کاربران (لایک/دیس‌لایک).
5. **وضعیت سرویس**
   - مسیر `/status` برای گزارش آماده‌بودن مدل، RAG و مخازن داده.
   - نمایش وضعیت در واسط کاربری و مدیریت خطاهای مدل با خلاصه‌ی جایگزین.
6. **دسترسی شبکه**
   - سرویس‌دهی رابط کاربری و API روی تمام اینترفیس‌ها تا کاربران شبکه داخلی بتوانند استفاده کنند.

## نیازمندی‌های فنی (Technical)
- **زبان و چارچوب**: Python 3.10+، Flask، HTML/CSS/JS تک‌صفحه‌ای.
- **مدل زبانی**: Ollama با مدل `gpt-oss:20b`، شامل fallback برای پاسخ‌های دترمینیستیک.
- **RAG و جست‌وجو**:
  - استفاده از `chromadb` برای ذخیره‌سازی بردارها.
  - چانکینگ داکیومنت‌ها با نرمال‌سازی Hazm و حفظ سر تیترها و ردیف‌های جدول.
  - جمع‌بندی دامنه‌ای CSV با گروه‌بندی و تولید خلاصه‌های آماده.
- **پردازش زبان**: `hazm`, `thefuzz`, `transformers`, `torch` برای امتیازدهی و تطبیق.
- **ساختار کد**: بسته‌ی `hr_ai` شامل مخازن داده، تحلیل‌گر نیت، سازنده‌ی زمینه و حل‌کننده‌ی پاسخ.
- **لاگ و تنظیمات**: فایل `config.json` برای پیکربندی مدل و آدرس Ollama؛ ماژول `logging_utils` برای ثبت پرسش‌ها.
- **رابط کاربری**: طراحی واکنش‌گرا، استفاده از فونت سفارشی (`hamishe.ttf`)، اسکرول خودکار تا پایین و نگه‌داشت کامپوزر.

## نیازمندی‌های غیر فنی (Non-Functional)
- **کارایی**: پاسخ‌دهی زیر ۳ ثانیه برای پرسش‌های بدون فراخوانی مدل؛ مدیریت همزمانی از طریق شمارنده‌ی درخواست‌ها.
- **دقت**: نرخ خطای پذیرفتنی برای نام‌پرس‌وجو < ۵٪، پاسخ‌های ناموجود باید صریحاً اعلام شوند.
- **قابلیت نگه‌داری**: کد ماژولار با تست Compile و امکان توسعه‌ی آسان Intentها و مخازن جدید.
- **امنیت و حریم خصوصی**: اجرای لوکال، عدم ارسال داده‌ها به سرویس‌های خارجی (به جز مدل روی دستگاه خودی).
- **تجربه کاربری**: رابط ساده، رنگ‌بندی مطابق برند، پشتیبانی از اسکرول خودکار و تعامل بی‌وقفه.
- **قابلیت دسترس‌پذیری**: کنتراست مناسب، کلیدهای میانبر (مثلاً ارسال با Enter)، پیام‌های خطای قابل فهم.

## وابستگی‌ها و داده‌ها
- فایل‌های ورودی: `policy_sections.json`, `policy_sections.json (نسخه فشرده)` (در صورت وجود)، `plist.xlsx` و هر فایل سیاستی تکمیلی.
- فونت سفارشی: `hamishe.ttf`.
- نیاز به دسترسی خواندن/نوشتن برای ایجاد `question_log.jsonl` و کش پایگاه برداری Chroma.

## سناریوهای حیاتی
1. **جست‌وجوی فرد**: «آیا حامد زمانی در شرکت است؟» → جست‌وجوی دقیق و بازگشت «پیدا نشد» در صورت نبود.
2. **آمار حوزه**: «معاونت نوآوری چند نفر دارد؟» → شمارش دامنه و نمایش نقش‌های غالب.
3. **مزایای سازمانی**: «هدیه تولد چقدر است؟» → استخراج بخش صحیح از سیاست‌ها.
4. **سفر و هزینه‌ها**: «حقوق من ۷۰ میلیون است، هزینه سفرم چقدر می‌شود؟» → محاسبه با فرمول تعیین‌شده یا درخواست داده‌ی بیشتر.

## مفروضات
- داده‌های CSV و DOCX به‌روز و بدون خطا هستند.
- سرویس Ollama در دسترس و مدل از پیش دانلود شده است.
- کاربران از طریق مرورگرهای مدرن (Chrome, Edge, Firefox) به برنامه متصل می‌شوند.

## برنامه‌ی توسعه آتی
- افزودن تست‌های واحد برای Intentها و مخازن داده.
- ذخیره‌ی بازخورد کاربران در دیتابیس پایدار به‌جای فایل ساده.
- اضافه‌کردن احراز هویت برای دسترسی خارج از شبکه‌ی داخلی.
- پشتیبانی از بارگذاری آنی اسناد جدید بدون نیاز به راه‌اندازی مجدد سرویس.
